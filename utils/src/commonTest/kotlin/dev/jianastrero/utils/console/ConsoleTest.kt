package dev.jianastrero.utils.console

import dev.jianastrero.utils.log.LogLevel
import io.kotest.core.spec.style.StringSpec

class ConsoleTest : StringSpec({
    "println should format message with tag and level" {
        // This is a common test that will be extended by platform-specific tests
        // The actual verification of println calls will be done in platform-specific tests
    }
})
